using System;

namespace QLChuoiNhaHangKhachSan.GUI.Repositories
{
    public static class InvoiceRepository
    {
        // Minimal placeholder used by BookingRoom_Details.
        public static void SaveInvoiceAndFinalize(string room, object info, object services, decimal total, object options)
        {
            // No-op placeholder to allow compilation. Real implementation should persist invoice and finalize booking.
        }
    }
}
